# 油猴脚本修改需求说明

## 一、修改目标

我想修改一个网页视频播放界面的油猴脚本，主要增强触屏和鼠标操作体验。

重点功能包括：

* 触屏手势控制视频
* 鼠标控制进度条显示和隐藏
* 侧边锁定按钮
* 侧边快进快退按钮
* Toast 提示
* 设置面板保存配置
* YouTube 兼容优化

---

## 二、参考效果说明

1. 目前我已经在 B 站网页端脚本中实现了这些效果。
2. 本次修改的目标效果，以我当前这个哔哩哔哩网页端脚本的实际效果为准。
3. 手势操作、进度条显示隐藏、Toast 提示、侧边按钮、音量增强等功能，优先参考当前 B 站脚本的表现。
4. 希望在其他视频网站上，也尽量实现和 B 站网页端相同或接近的使用效果。
5. 如果其他网站因为播放器结构不同，无法完全做到一致，请提前说明，并尽量采用兼容方案。

---

## 三、适用范围

1. 脚本尽量适用于主流视频网站。
2. 重点兼容 YouTube。我在测试过程中发现 YouTube 可能会有页面动态加载、切换视频后脚本失效、脚本注入或事件监听不稳定的问题。
3. 需要支持普通播放页面和全屏播放页面。
4. 不要影响网页原本的按钮、输入框、评论区等非视频区域操作。
5. 如果某些网站无法完全兼容，请提前说明。

---

## 四、进度条显示和隐藏

这是最重要的功能之一。

1. 触屏单击视频区域时，显示进度条。
2. 鼠标进入视频区域时，显示进度条。
3. 鼠标在视频区域移动时，进度条持续显示。
4. 没有操作后，进度条 3 秒后自动隐藏。
5. 鼠标离开视频区域后，进度条立即隐藏。
6. 锁定状态下，应立即隐藏进度条。
7. 我目前的进度条显示和隐藏，是通过模拟鼠标事件来实现的：
   * 显示进度条时，模拟鼠标移动到视频区域内。
   * 隐藏进度条时，模拟鼠标移动到视频区域外。
8. 如果模拟鼠标事件的方式不合适，可以改成更稳定的实现方式，但最终效果要尽量接近原播放器进度条。

---

## 五、触屏手势功能

### 1. 单击

1. 单击视频区域时，只显示进度条。
2. 单击不要触发播放、暂停、快进、亮度、音量等功能。

### 2. 双击

1. 双击视频区域时，切换播放和暂停。
2. 播放中双击后暂停。
3. 暂停中双击后播放。
4. 双击不要误触发两次单击。

### 3. 长按

1. 长按视频区域时，进入倍速播放。
2. 手指松开后，恢复正常速度。
3. 长按倍速数值可以在设置面板修改。
4. 长按时需要显示 Toast 提示。

### 4. 横向滑动

1. 横向滑动用于调节播放进度。
2. 向右滑动快进。
3. 向左滑动后退。
4. 快进、后退秒数可以在设置面板修改。
5. 滑动时需要显示 Toast 提示。

### 5. 纵向滑动

1. 视频左侧上下滑动调节亮度。
2. 视频右侧上下滑动调节音量。
3. 向上滑动增加，向下滑动降低。
4. 亮度和音量都需要显示 Toast 提示。
5. 亮度和音量最大值需要能超过 100%。
6. 音量超过 100% 希望继续用 Web Audio API 实现。
7. 最大音量倍数可以在设置面板修改。

---

## 六、手势冲突处理

需要避免各种手势互相误触发。

1. 单击、双击、长按、横向滑动、纵向滑动之间不能冲突。
2. 双击不要触发两次单击。
3. 长按不要触发单击或双击。
4. 横向滑动不要触发纵向滑动。
5. 纵向滑动不要触发横向滑动。
6. 一旦判断为某一种手势，其他手势应取消。
7. 锁定状态下，除了点击解锁按钮，其他所有手势都要失效。

---

## 七、侧边锁定按钮

1. 视频区域侧边需要有锁定按钮。
2. 点击后进入锁定状态。
3. 锁定后立刻隐藏进度条。
4. 锁定状态下，所有手势都要失效。
5. 锁定状态下，只有解锁按钮可以点击。
6. 解锁按钮需要一直显示，方便解锁。
7. 解锁后，所有手势恢复正常。
8. 锁定和解锁时需要显示 Toast 提示。
9. 锁定按钮需要跟随进度条显示和隐藏。
10. 但锁定状态下，解锁按钮不能隐藏。

---

## 八、侧边快进快退按钮

1. 视频区域侧边需要有快进、快退按钮。
2. 按钮可以展开和收起。
3. 展开后，点击按钮可以快进或快退。
4. 快进、快退秒数可以在设置面板修改。
5. 按钮需要跟随进度条显示和隐藏。
6. 按钮不要影响视频区域的正常手势。
7. 点击快进或快退时需要显示 Toast 提示。

---

## 九、Toast 提示

1. Toast 样式尽量保持原脚本风格。
2. 需要Toast提示的操作可以参考原 B 站网页端脚本。

---

## 十、设置面板

1. 设置面板样式尽量保持原来的样子，原有选项尽量不要删除。
2. 设置修改后需要自动保存，刷新网页后仍然生效。
3. 如果新增功能需要增加设置项，可以加到原来的设置面板里。

---

## 十一、代码优化要求

1. 可以在不影响原有功能的前提下，优化我现有脚本中的代码。
2. 如果我原来的实现方式不够稳定，可以改成更稳定的方式。
3. 但优化时不要破坏原本已有功能。
4. 设置面板的样式和现有选项尽量保持不变。

---

## 十二、重点要求

请优先保证以下功能：

1. 进度条显示和隐藏稳定。
2. 单击、双击、长按、横向滑动、纵向滑动都能正常使用。
3. 各种手势之间不能冲突。
4. 锁定按钮稳定，锁定后所有手势失效。
5. 解锁后所有手势恢复。
6. 快进快退按钮能正常展开、收起和使用。
7. 音量最大值能超过 100%，最好继续使用 Web Audio API 实现。
8. Toast 提示保留原来的风格。
9. 设置面板样式和选项尽量保持原样。
10. YouTube 页面脚本可以正常工作，切换视频后也要继续生效。
11. 最终效果尽量和我当前 B 站网页端脚本的效果保持一致。
